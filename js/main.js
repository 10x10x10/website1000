
import {regHeader, regProjectView} from "./components.js";

regHeader();
regProjectView();

const app = new Vue({
    el: '#app',
    data: { },
    components: { },
    mounted: function(){ },
    methods: { }
});
